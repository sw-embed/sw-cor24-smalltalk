# Step: v0-release-notes

When D1, D2, D3 all run cleanly and the upstream-dependent
refactors are either landed or accepted as deferred:

1. Write `README.md` modelled after
   `../sw-cor24-basic/README.md`. Cover: overview, architecture
   diagram (5-layer), dialect, demos, build/run, dependencies.
2. Update `docs/status.md` to reflect what shipped.
3. Tag `v0.0.1` and push.

This is documentation-only. No code changes.
